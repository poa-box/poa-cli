/**
 * Moved to @poa-box/core (browser-pure). This shim keeps the historical CLI import
 * path — and the `@poa-box/cli/lib/contracts` subpath the agent deep-imports — stable.
 */
export * from '@poa-box/core/contracts';
