/**
 * Moved to @poa/core (browser-pure). This shim keeps the historical CLI import
 * path — and the `@poa/cli/lib/encoding` subpath the agent deep-imports — stable.
 */
export * from '@poa/core/encoding';
