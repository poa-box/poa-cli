/**
 * Moved to @poa/core (browser-pure). This shim keeps the historical CLI import
 * path — and the `@poa/cli/lib/label-aliases` subpath the agent deep-imports — stable.
 */
export * from '@poa/core/label-aliases';
